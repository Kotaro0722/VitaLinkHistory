import { TextField } from "@mui/material";

const TextInput = () => {
  return (
    <TextField
      id="outlined-basic"
      sx={{ width: "100%", backgroundColor: "white" }}
    />
  );
};

export default TextInput;
